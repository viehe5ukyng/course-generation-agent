请在不破坏原有课程结构的前提下，根据以下改进要求修订 Markdown 主稿。

只返回修订后的完整 Markdown。
这次修订必须满足下面四条：
1. 必须严格执行所有约束，尤其是用户明确排除的案例、场景、表达方式，不能再次出现
2. 必须基于原稿做出看得见的新变化，不能只是换几句话后整体结构和案例完全不变
3. 如果原稿里有明显更好的段落或逐字稿表达，要尽量保留这些优点，不要为了改而把质量改差
4. 如果修订目标要求替换案例，就要连同案例目标、步骤、逐字稿中的对应内容一起改掉，不能只改标题

课程上下文：
{context_summary}

基础版本：
v{source_version}

本轮修订目标：
{revision_goal}

必须遵守的约束：
{constraint_summary}

改进要求：
{approved_changes}

当前 Markdown：
{markdown}
